---
const { products, id } = Astro.params

const URL = Astro.url.host
console.log(URL)

const response = await fetch('https://randomuser.me/api/')
const data = await response.json()
const randomUser = data.results[0].email
console.log(randomUser)
---

<h1>{products}: {id}</h1>
<p>
  {randomUser}
</p>
<h1>Products</h1>
